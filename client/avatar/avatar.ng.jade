//-img.profile-picture-mini(
  ng-src='{{ Avatar.getAvatarUrl() }}',
  width='{{ Avatar.width || "auto" }}',
  height='{{ Avatar.height || "auto" }}')

.avatar-wrapper(ng-style="{'background-image':'url('+ Avatar.getAvatarUrl() +')'}")
