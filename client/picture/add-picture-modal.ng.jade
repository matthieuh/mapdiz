md-dialog.add-picture-modal
  form
    md-toolbar
      .md-toolbar-tools
        h2 Ajouter une image
    md-dialog-content.p2

      .drop-box(ng-click='addPicture($event)', ng-hide='cropper.sourceImage')
        div Cliquez dans cette zone pour ajouter une image
        input(
          type='file',
          img-cropper-fileread,
          image='cropper.sourceImage',
          style="display:none"
        )
      .crop-container(ng-show='cropper.sourceImage')
        h3 Bravo ! Maintenant redimensionnez votre image ...

        .crop-zone
          .cropper
            canvas(
              width='522px',
              height='200px',
              image-cropper='',
              image='cropper.sourceImage',
              cropped-image='cropper.croppedImage',
              crop-width='522',
              crop-height='200',
              keep-aspect='true',
              touch-radius='30',
              crop-area-bounds='bounds'
            )
          .preview(ng-show='cropper.croppedImage!=null')
            img(ng-src='{{cropper.croppedImage}}')

    .md-actions(layout='row')
      span(flex='')
      md-button.md-primary(ng-click='answer(cropper.croppedImage)', ng-disabled='!cropper.sourceImage')
        | Ajouter Image
      md-button.md-warn(ng-click='answer()')
        | Annuler
