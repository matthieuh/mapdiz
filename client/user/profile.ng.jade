header.flex.p2
  .avatar-container
    //- img.profile-picture(src='http://geoadrs.com/image/default-user.png', ng-if='!vm.profilePic.url')
    img.avatar(ng-src='{{ Profile.avatar.url("avatar-large") }}')
    input#avatar-input(
      type="file",
      ng-show='false'
    )
    .edit-avatar-btn(ng-click='Profile.openAvatarInput()') Changer
  infos.ml2.flex-auto
    h1 Mon Compte
    hr
    p Membre depuis le {{ Profile.currentUser.createdAt | date: 'dd/M/yyyy' }}
    p E-mail : {{ Profile.currentUser.emails[0].address }}

section {{ Profile.currentUser | json }} {{ Mapdiz.avatar | json }}

